# Ajar — общий TODO

Статусы реализации по блокам — в `STATUS.md`. Здесь — всё, что вне блоков.

## Этап 1 — free-first релиз (текущий приоритет)

Решение 2026-09-01: Paddle не готов, релиз его не ждёт. Выходит бесплатная версия, всё разблокировано, исходники открыты. Продуктовое обоснование — `docs/PRODUCT.md` «Этапы выпуска». Порядок блоков — `docs/plans/ROADMAP.md`.

- [x] Лицензия репозитория выбрана — **PolyForm Noncommercial 1.0.0**, файл `LICENSE` в корне.
- [x] `README.md` в корне — витрина открытого репо (питч, статус, сборка, требования, «почему открыто»).
- [x] **Block 12 (код):** `tier` → `.pro` принудительно (`AppState.proUnlockedForFreeStage`); триал, пейволл, счётчик апселла убраны из UI; код `Ajar/Licensing/` и `PaywallView.swift` остались в дереве; тесты чистой логики оставлены. 129 тестов зелёные.
- [x] **Репозиторий публичный** — 2026-09-01, `https://github.com/rilya888/ajar`, topics `macos menubar swift swiftui lid-angle macos-app iohidmanager`. История перед публикацией схлопнута в один коммит (в старой были Paddle ID и партнёрская почта в диффах); полная история — локально в теге `pre-public-squash`. Операционные детали Paddle вынесены в `docs/private/paddle-etap2.md` (в `.gitignore`).
- [x] **Бесплатный DMG залит** — 2026-09-01. `Tools/release.sh` (агент починил 2 бага в нём: SIGPIPE в grep, `set -e` вокруг `osascript`), нотаризация `.app` и DMG — Accepted+stapled, `spctl` → `Notarized Developer ID`. `Ajar-1.0.dmg` (1369921 B) + `appcast.xml` (edSignature на месте) лежат в `QuietUnit/site/public/ajar/`, задеплоены. `curl -I` → 200 на оба; SHA-256 live == local. Фон окна DMG не лёг (нет Automation→Finder в неинтерактиве) — DMG валиден, косметика.
- [x] **Лендинг под free-first** — 2026-09-01, задеплоен на `quietunit.com/ajar`. Цена/покупка/триал сняты, `Buy` → `Download` на `/ajar/Ajar-1.0.dmg`, `/ajar/buy` → редирект на `/ajar`, merchant of record и ссылки `/terms` `/refund` убраны из тела (страницы и футер целы), добавлена кнопка `Source` на репо + строка про PolyForm. Коммиты QuietUnit `ffa8d0e`, `bb91da1`.
- [ ] **GTM** (раздел внизу файла) — Show HN, r/macapps и прочее делаются на Этапе 1: бесплатная версия + открытый репо это ровно то, что там просят.
- [ ] **Ручные проверки релиза** (руки владельца, раздел «Релиз 1.0» ниже): бэкап приватного ключа Sparkle, Gatekeeper на чистом пользователе (скачать DMG браузером), реальное обновление Sparkle с предыдущей версии, полный smoke по `docs/release/checklist.md`.

## Этап 2 — Pro (позже, когда налажена оплата)

- [ ] **Block 13 (код):** вернуть триал и пейволл флагом (антипод Block 12), проверить откат `tier` на живом триале.
- [ ] Довести Paddle live — все незакрытые подпункты Paddle ниже в этом файле относятся сюда и **Этап 1 не блокируют**.
- [ ] Пересмотреть триал 14 дней и цену $9 до первой продажи (открытые вопросы внизу).
- [ ] Гифка жеста — нужна обоим этапам, сценарий съёмки ниже.

## Задачи владельца (блокируют релиз, не блокируют разработку)

- [x] **Решения по идентичности приложения приняты владельцем 2026-08-24, до подписи и первых установок:** bundle ID `com.ajarmac.ajar` → **`com.quietunit.ajar`**, `MARKETING_VERSION` `0.1.0` → **`1.0`**. Обе правки делает Block 11 первым шагом; после первой подписанной установки они уже не отыгрываются (уедут Keychain-item с лицензией и `UserDefaults` с датой триала).
- [x] **Подпись и нотаризация готовы 2026-08-24 — Block 11 больше ничем не заблокирован.**
  - `Developer ID Application: OLEKSANDR RYLKOV (D89KDAZ648)` выпущен и лежит в связке ключей (`security find-identity -v -p codesigning` — пятая идентичность, валидна, приватный ключ на месте). До этого были только три `Apple Development` (team `K3L3BZP54H`) и `Apple Distribution` (Mac App Store, для прямой раздачи не годится).
  - **Team ID для релиза — `D89KDAZ648`**, тот же, под которым у владельца выложены приложения в App Store Connect. Он уходит в подпись и в цепочку обновлений Sparkle: смена после релиза ломает автообновление у всех установленных копий.
  - Профиль нотаризации сохранён в связку ключей под именем **`ajar`** (`xcrun notarytool store-credentials`, app-specific password с appleid.apple.com). Проверка: `xcrun notarytool history --keychain-profile ajar` → «No submission history», то есть авторизация проходит. Дальше только `--keychain-profile ajar`, сам пароль нигде не всплывает и в контекст ИИ-сессии не попадает.
- [x] **Имя выбрано: Ajar** (2026-08-11). Рабочее название Hinge отброшено из-за приложения для знакомств.
- [x] **Домен решён 2026-08-13: не поддомен, а путь на существующем `quietunit.com`** — `https://quietunit.com/ajar` (сайт с остальными продуктами владельца — CaloriGram, SleepOff и др., статика на Astro). Новый домен не покупаем, DNS не трогаем. **Следствие: bundle ID меняется** с `com.ajarmac.ajar` на `com.quietunit.ajar` (или как называются bundle ID других приложений на этом сайте — сверить перед Block 11, до подписи и первых установок). Privacy и Support — общие страницы сайта уже существуют, для Ajar нужен только свой раздел на них, отдельных страниц заводить не надо. Закрывает и хостинг лендинга — отдельного пункта не нужно.
- [x] Папку проекта переименовать в `Ajar` — сделано, репозиторий лежит в `/Users/mac/Projects/Ajar`.
- [x] **Apple Developer Program** — есть (2026-08-11). Нужен для Developer ID и нотаризации в Block 11.
- [x] **Провайдер платежей — Paddle, подтверждён Billing** (2026-08-13). Офлайн-схема — вебхук подписывает ключ Ed25519, приложение проверяет локально `CryptoKit`, сервер активации не нужен. Block 09 реализован по ней.
- [x] **Ключевая пара Ed25519 для лицензий** — сгенерирована 2026-08-13. Публичный ключ вписан в `LicenseVerifier.publicKeyBase64`, приватный держит владелец вне репозитория (уйдёт в Worker secret, когда появится пункт ниже). Тестовые фикстуры в `AjarTests/LicensingTests.swift` отвязаны от боевого ключа — сверяются со своим `fixtureKey`, переданным явным параметром, а не дефолтом. Все 13 тестов `LicenseVerifierTests` зелёные.
- [ ] **[ЭТАП 2] Paddle live — операционные детали вынесены в `docs/private/paddle-etap2.md`** (не в публичном репо: product/webhook ID, адрес воркера, партнёрский аккаунт). Возвращается на Block 13. Резюме: каталог и вебхук на live заведены, юр-страницы `/terms` `/refund` есть; осталось — одобрение домена в дашборде Paddle, верификация аккаунта (KYC/выплаты), живой `PUBLIC_PADDLE_TOKEN`, контрольная покупка.
- [x] **Пейволл проверен руками 2026-08-24** на Release-сборке в `/Applications`. Триал (первый запуск 15.08, 9 дней использовано, 5 осталось) → Settings ▸ About → «Unlock Pro…» открывает окно «Ajar Pro». Из попапа на триале входа в пейволл нет и не должно быть: `offersUpgrade = tier == .free` ([`MenuBarViewModel.swift:67`](../Ajar/UI/MenuBarViewModel.swift)), решение осознанное и покрыто тестом. Активация ключа тоже подтверждена вживую: About показывал `Licensed to rilya888@gmail.com`, пикеры шорткатов были разлочены, снятие лицензии корректно вернуло тариф к живому триалу (лицензия поверх триала, а не вместо него). **Block 09 закрыт полностью.**
  - Открытый продуктовый вопрос: покупка на триале доступна только через Settings ▸ About. Если гипотеза «покупают на 2–3 день» верна, стоит сделать строку `Trial · all features` в попапе ссылкой на пейволл — одна строка плюс правка теста, обратного отсчёта это не добавляет. Владелец 2026-08-24 оставил текущее поведение.
- [x] **Иконка приложения** — нарисована 2026-08-13 по прямой просьбе владельца (прежняя формулировка «владелец рисует сам» снята им же). Мотив: MacBook сбоку с приоткрытой крышкой (~55°) — он же знак угла «∠», то есть ровно то, чем управляет продукт. **SF Symbols не использованы** (запрещены лицензией Apple в иконках) — все формы рисуются примитивами.
  - Генератор — `Tools/make_icon.py`, PIL, мастер рендерится в 4× и даунсемплится (PIL не сглаживает свои примитивы). Четыре цветовые схемы: `graphite` (собрана), `indigo`, `amber`, `silver`. Пересборка/перекраска: `python3 Tools/make_icon.py <схема> Ajar/Resources/Assets.xcassets/AppIcon.appiconset`, затем `xcodebuild`. **Нужен arm64-питон**: `/usr/local/bin/python3` на этой машине x86_64 и Pillow под ним не грузится, рабочий — `/opt/anaconda3/bin/python3`.
  - Толщина штриха (100 в дизайн-пространстве 1024) подобрана не на глаз, а по слоту 16×16: тоньше — мазня. Проверены все размеры вплоть до 16 px.
  - `Ajar/Resources/Assets.xcassets/AppIcon.appiconset/` — 10 PNG + `Contents.json`; в проект добавлен `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon` (обе конфигурации приложения, тестовая не тронута). Проверено на собранном бандле: `AppIcon.icns` внутри, `CFBundleIconName = AppIcon`, картинка вытащена из бандла и сверена глазами.
  - Отдельный `docs/release/Ajar.icns` — для DMG и лендинга. **В бандл он не входит намеренно**: сначала лежал в `Ajar/Resources/` и синхронизированная группа Xcode затащила его внутрь приложения лишними 165 КБ.
- [x] **Два дефолтных Shortcut'а для зоны «упор» — сделано и проверено на железе 2026-08-16.** Оба собраны, подписаны `shortcuts sign -m anyone`, залиты на `quietunit.com/ajar/shortcuts/do-not-disturb-{on,off}.shortcut` (живой сервер — на сайте нет CI/CD, push в git ничего не деплоит) и в репозиторий QuietUnit. Установлены кнопкой из онбординга, `shortcuts run` даёт `exit 0`, владелец подтвердил глазами: режим фокуса реально включается и выключается.
  - Действие — встроенное `is.workflow.actions.dnd.set`, параметры `Enabled` (bool) + `FocusModes` (`DisplayString` / `Identifier` = `com.apple.donotdisturb.mode.default`). Список встроенных действий этой macOS добывается грепом `is\.workflow\.actions\.` по `dyld_shared_cache_arm64e.*` — единственный надёжный способ проверить, что действие вообще существует, до импорта такое не ловится.
  - **Тупики, не повторять:** `is.workflow.actions.donotdisturb.set` не существует; App Intent `com.apple.donotdisturb.DoNotDisturbAppIntents.SetFocusState` (из `DoNotDisturbAppIntents.appex`) реестр WorkflowKit грузить отказывается — `Failed to load a definition`, дальше `WFMissingAction`, хотя `pluginkit` провайдер видит. Оба варианта импортируются без единой жалобы и падают только при запуске.
  - Имена шорткатов (`Ajar Do Not Disturb` / `Ajar Resume Focus`) — то, чем их зовёт `/usr/bin/shortcuts run`, и одновременно имя скачиваемого файла. После релиза менять нельзя без миграции привязок; двоеточия в них быть не должно (на macOS это разделитель пути в именах файлов).
- [ ] **Гифка жеста для лендинга — сценарий съёмки (руки владельца).** Главный элемент продажи, важнее всего текста на странице. Не композит из двух источников (рука отдельно, экран отдельно) — один живой кадр, где видно и руку, и экран разом: продукт продаёт не картинка, а доверие, что софт правда реагирует на угол крышки.
  - **HERO (главная гифка, 3–4 сек, петля).** Камера сбоку-сзади ноута, ~30–40°, в кадре рука + угол экрана с меню-баром. Яркость экрана на максимум (компенсирует засветку камеры), ноут развернуть от окна — без бликов на стекле. Действие: рука входит, толкает крышку до угла мьюта, доля секунды паузы (дать иконке смениться), крышка обратно — иконка обратно. Первый и последний кадр почти совпадают — иначе петля дёргается.
  - **INSERT (макро-вставка иконки).** Отдельная реальная запись QuickTime (⌘⇧5, выделенный фрагмент — сам угол меню-бара), 1–2 сек, врезается сразу после смены угла в HERO. На общем плане иконка физически нечитаема — общий план продаёт достоверность, макро продаёт факт.
  - **SECOND GESTURE.** Тот же ракурс, тот же свет, тот же ноут — консистентная серия. Толчок крышки до упора → Shortcut (DND/Focus). Пейофф — Focus-баннер / иконка режима в Control Center, тоже с макро-вставкой.
  - **Не снимать:** Zoom/Meet с «погасшим» индикатором мика — сайт сам честно пишет, что их UI не гаснет (`site.md`, пункт 6). Такой кадр — прямое враньё, ровно то, от чего лендинг специально отбивается.
  - **Техника:** телефон на штативе, 4K60, потом кроп под нужный кадр. 5–6 дублей, взять естественный, не дёрганый. В кадре только ноут+стол+рука. Сборка: `ffmpeg` two-pass palette (см. ниже) либо немой autoplay `<video>` вместо `.gif` (чётче, легче файлом, петля ровнее).
  - Кода/имитации быть не может — реальная запись, реальное прикрытие крышки до срабатывания мьюта. `ffmpeg` two-pass:
    ```
    ffmpeg -i input.mov -vf "fps=15,scale=800:-1:flags=lanczos,palettegen" palette.png
    ffmpeg -i input.mov -i palette.png -filter_complex "fps=15,scale=800:-1:flags=lanczos[x];[x][1:v]paletteuse" output.gif
    ```
- [x] **Таблица совместимости — решено окончательно 2026-08-15, замера на железе не будет.** Владелец подтвердил повторно: три модели имеют именно **датчик угла** (градусы, не просто открыто/закрыто) — MacBook Pro 16" (2019), MacBook Pro 14" (2021), MacBook Air (2022). Машин физически нет и не будет в обозримое время, ждать их бессмысленно. Решение блока (CLAUDE.md про «не имитировать замеры») остаётся в силе для *чисел* (углы, тайминги, пороги) — там имитации по-прежнему нет и не будет. Но присутствие/отсутствие самого датчика на конкретной модели — не измеряемая в этом проекте величина, а факт про железо, который владелец знает независимо. На лендинге колонка «кто подтвердил» для этих трёх строк — **«by developer»** (не «measured»), это честно отражает происхождение данных и не выдаёт его за замер `lidangle.swift`. Если пользователи начнут присылать отчёты — таблица растёт их числом, эти три строки не трогать.

## Релиз 1.0 — руки и аккаунты владельца (Block 11)

Конвейер собран и проверен: `Tools/release.sh` доводит исходники до нотаризованного DMG и `appcast.xml` одной командой. Ниже — только то, что ИИ-сессия сделать не может.

- [ ] **Залить релиз.** `Tools/release.sh`, затем `build/release/dist/Ajar-1.0.dmg` и `appcast.xml` — в `quietunit.com/ajar/` (тот же rsync, которым выкладываются `shortcuts/*.shortcut`; CI нет). Проверка: `curl -I https://quietunit.com/ajar/appcast.xml` → 200, сейчас 404. Без этого файла автообновление у установленных копий будет молча получать 404 раз в сутки.
- [ ] **Забэкапить приватный ключ Sparkle.** Он существует в единственном экземпляре — в login keychain этой машины (generic password, сервис `https://sparkle-project.org`, аккаунт `ed25519`). Потеря ключа = обновить установленные копии больше нечем, только просить переустановить руками. Скопировать в менеджер паролей.
- [ ] **Gatekeeper на чистом пользователе.** Скачать DMG **браузером** (копирование по файловой системе карантинный атрибут не ставит и ничего не проверяет), открыть под другим пользователем macOS. Ни одного предупреждения. Это единственная честная проверка нотаризации; на машине разработки она невозможна.
- [ ] **Реальное обновление Sparkle.** Поставить 1.0, поднять `MARKETING_VERSION` до 1.0.1, снова `Tools/release.sh`, положить новый appcast на место фида, дождаться (или дёрнуть руками через Settings ▸ About ▸ Check for Updates…) — приложение должно найти, скачать, поставить и перезапуститься, сохранив настройки и лицензию. Сломанный апдейтер невозможно починить апдейтом, поэтому пункт не пропускается.
- [ ] **Полный smoke** по `docs/release/checklist.md` на том же чистом пользователе.
- [ ] **Гифка жеста на лендинг** — сценарий съёмки ниже в этом файле, он не изменился.

### Что владельцу нужно знать про смену bundle ID

Bundle ID сменился с `com.ajarmac.ajar` на `com.quietunit.ajar`, версия — с 0.1.0 на 1.0. Для новой сборки это другое приложение, поэтому на этой машине:

- **Живой триал (первый запуск 15.08) не переедет** — новая сборка начнёт свои 14 дней с нуля. Это правильно и ожидаемо.
- **Активированная лицензия не переедет**: ключ лежит в Keychain под service `com.ajarmac.ajar`, новая сборка читает `com.quietunit.ajar`. Вернуть Pro — заново вставить тот же ключ из письма в пейволл (Settings ▸ About → «Unlock Pro…»). Ключ бессрочный и не привязан к машине, повторная активация ничего не стоит.
- Старая копия в `/Applications` с прежним bundle ID продолжит работать сама по себе и будет вторым пунктом в меню-баре. Перед установкой новой: `osascript -e 'quit app "Ajar"'`, затем удалить её и, если нужно чисто, `defaults delete com.ajarmac.ajar`.

## Один проход крышкой (руки владельца, ~15 минут)

Ручные чек-листы блоков 02, 04, 05, 06, 07 требовали одного и того же — живой крышки — и лежали пятью отдельными пунктами. Собраны здесь в один сеанс, порядок подобран так, чтобы состояния не мешали друг другу. Проходить сверху вниз, отмечать по ходу.

Подготовка: `xcodebuild -scheme Ajar build`, запустить `Ajar.app` из DerivedData. Рядом держать терминал.

- [x] **1. Калибровка, отрицательный путь** — пройдено 2026-08-12. Отказ показан, `maxAngle` не записан.
- [x] **2. Калибровка, положительный путь** — пройдено 2026-08-12. Записан **131°**, то же число, что в свипе Block 01. Проверено не глазами: значение прочитано из `UserDefaults`.
- [x] **3. Живой индикатор** (Block 07 п.1) — пройдено 2026-08-12. Бегунок идёт за крышкой. *Оговорка:* сверка чисел «попап против `lidangle.swift` в пределах градуса» и замер задержки < 100 мс отдельно не делались — отставания на глаз не видно, но числом это не подтверждено (Block 02 п. 1–2 остаются частичными). Заодно снят дефект формулировки: во вкладке Zones **нет** переключающейся подсветки зоны, там цветная полоса с фиксированными сегментами и бегущая по ней риска. Подсветка текущей зоны живёт в попапе меню-бара.
- [x] **4. Иконка и попап** (Block 06) — пройдено 2026-08-12 **глазами владельца**: все состояния иконки отображаются верно. Скриншотом не подтверждено: снять меню-бар не удалось, полноэкранное окно его прячет (глобальное автоскрытие при этом выключено). Если понадобится доказательство в картинках — снимать при выходе из full screen.
- [x] **5. Сквозной жест — главный пункт** (Block 04 п.1 и п.7) — **пройдено 2026-08-12, с числами**. Состояние микрофона снималось прямо из CoreAudio параллельно с углом, лог — `docs/research/gesture-fixed-2026-08-12.log`: мьют через **1,115 с** после остановки крышки на 29°, восстановление через **1,24 с** после остановки в рабочем положении. Оба в бюджете (`dwell` 1,0 с + тик 0,1 с + опрос 0,05 с + CoreAudio 4 мс). Подтверждён и `stabilityDelta`: дребезг 29↔30 во время удержания окно не сбросил. **Первый прогон этого пункта вскрыл главный дефект проекта** — см. «Последний результат» в `STATUS.md`.
- [x] **6. Тумблер паузы** (Block 05 п.3) — **пройдено 2026-08-12, с числами** (`docs/research/pause-2026-08-12.log`): на паузе крышка простояла в зоне `low` **8,9 секунды** и мьюта не случилось; после снятия паузы тот же жест дал мьют через 1,15 с и восстановление через 1,10 с. Попутно подтвердилась «честность» восстановления из Block 05: выход из `low` на паузе вызвал `restore()`, но записи в CoreAudio не произошло — мьютить было нечего.
- [x] **7. Стартовая пауза** (Block 05 п.2) — **пройдено 2026-08-12** (`docs/research/startup-grace-2026-08-12.log`). Приложение запущено при крышке на 37°, и она простояла в зоне `low` **72 секунды** — мьюта не случилось ни разу. Механика: переход `normal → low` зафиксировался через секунду после старта, попал в трёхсекундную грацию и был подавлен, новых переходов не возникало — догона нет по конструкции. Затем крышка открыта и снова прикрыта: микрофон замьючен, то есть приложение живо, а не мертво. *Оговорка:* оба логгера истекли за 4 с до второго удержания, момент мьюта в логе не записан — зафиксировано итоговое состояние `MUTED`. Для этого пункта достаточно: вопрос был «жив ли», а не «за сколько».
- [x] **8. Сон** (Block 05 п.1, Block 02 п.3) — **пройдено 2026-08-12** (`docs/research/sleep-2026-08-12.log`), все три подпункта. (1) *Полное закрытие не мьютит:* крышка прошла зону `low` за 0,4 с при закрытии 112° → 0° за 1,5 с, требование неподвижности её отсекло. Это первое подтверждение на живом железе — в Block 03 дефект из замера №15 закрывали прогоном записанного лога через `ZoneTracker`. (2) *Микрофон пережил сон:* логгер состояния печатает только изменения и за весь прогон выдал одну строку — `live` до закрытия, `live` после пробуждения. (3) *Датчик ожил сам* после 132 с сна, чистыми значениями с первого отсчёта (11° → 32° → 61° → 100°), без перезапуска приложения; в системном логе Ajar за это время ни одной строки, то есть removal-колбэков не было.
- [x] **9. Внешние микрофоны** (Block 04 п.2) — **пройдено 2026-08-12 на AirPods** (`docs/research/airpods-2026-08-12.log`). AirPods стали устройством ввода по умолчанию (id 103, transport `blue`), путь — **системный `Mute`**, не fallback на громкость: `has=true settable=true`. Сквозной жест дал мьют через **1,13 с** после остановки крышки на 29° и восстановление через **~1,0 с** после возврата в рабочее. Замьючены именно AirPods — в логе стоит id 103, встроенный микрофон (id 90) не трогали. USB-микрофона в наличии нет, остаётся непроверенным классом. **Находка для Block 10:** микрофон iPhone через Continuity (id 95, transport `ccwd`) не поддерживает **ни** `Mute`, **ни** `VolumeScalar` — замьютить его нечем в принципе. Онбординг обязан это назвать, иначе пользователь увидит «сработало» в иконке и говорящий микрофон в звонке.
- [x] **10. Clamshell** (Block 01/05) — **пройдено 2026-08-12 на мониторе кабелем** (`docs/research/clamshell-2026-08-12.log`), три сценария. **Вывод: ветка `CGDisplayIsBuiltin` не нужна, комментарий в `Suppressor.swift` поправлен.** (A) *Быстрое закрытие:* машина не уснула (датчик читался все 94 с), микрофон не тронут ни разу. (B) *Медленное закрытие:* мьют через 1,09 с после того, как крышка притормозила на 23°, и восстановление через 1,45 с после укладки на 0° — микрофон замьючен **2,67 с** посреди закрытия и размьютился сам переходом `low → closed`. Это **не clamshell-специфично**: то же самое происходит без монитора, просто там машина в этот момент уже спит. Прежний комментарий утверждал, что «путь через `low` режется требованием неподвижности» — это верно только для быстрого закрытия. (C) *Крышка приоткрыта на 18°:* мьют через 0,93 с и держится все оставшиеся 80 с, включая сползание крышки 18° → 28° внутри зоны `low`. Правильное поведение: это жест.
  - Побочные факты: закрытая крышка даёт не «ровно 0°», а дребезг **0↔1°** (Block 01 писал «ровно 0°»); `closedBelow = 3` его накрывает. Зеркалирование считается **одним** экраном в `NSScreen.screens`, но `CGGetOnlineDisplayList` видит два, а единственный активный дисплей отдаёт `builtin=false` — то есть дискриминатор «есть не-встроенный» работал бы и в зеркале, если бы понадобился.

## Открытые вопросы (решить по ходу)

- [x] ~~Block 01: отличима ли закрытая крышка от прикрытой~~ — отвечено 2026-08-11: закрыто = ровно 0°, последнее ненулевое = 4°, разрыв разделяет состояния сам.
- [x] ~~Block 01: переживает ли чтение сон~~ — отвечено 2026-08-11: да, полностью, переоткрытие устройства не нужно.
- Все пункты, упиравшиеся в движение крышки и внешний монитор (блоки 01, 02, 04, 05, 06, 07), сведены в раздел «Один проход крышкой» выше — 2026-08-12.
- [ ] **Block 08:** если задержка запуска шортката окажется больше ~700 мс, жест начнёт ощущаться вялым. Тогда обсудить возврат к встроенным действиям для паузы медиа — ценой запроса Accessibility.
- [x] ~~**Block 04:** поддерживает ли встроенный вход `kAudioDevicePropertyMute`~~ — отвечено 2026-08-11: да. `MacBook Pro Microphone` (id 90) отдаёт `Mute` как present **и** settable, fallback на громкость не потребовался. Текст онбординга можно писать про «system mute», не про «volume to zero».
- Синтетический клик по `NSStatusItem` попап не открывает (проверено в Block 06: `AXPress` проходит без ошибки, окна не появляется). Визуальная проверка попапа возможна только руками — это не чинится, это ограничение среды.
- [ ] Триал 14 дней — гипотеза. Пересмотреть после первых отчётов: если люди покупают на 2–3 день, две недели избыточны.
- [ ] Цена $9 — до первой продажи меняется свободно, после уже нет (купившие остаются с лицензией). Пересмотреть до запуска, не после.
- [x] ~~**Дефект `CalibrationSession.minimumValidMax`**: калибровка из рабочего положения записывала 120° как «упор»~~ — исправлено 2026-08-12. Порог 90° сам по себе неисправим: на этой машине рабочее положение (115–120°) выше любого порога, который пропустил бы упор (131°), а на другой модели упор может лежать ниже чужого рабочего положения. Добавлен второй признак — `minimumRise = 5`: угол обязан **вырасти** за окно измерения. Нажал кнопку и не двинул крышку — отказ, независимо от абсолютного числа. Проверка руками — п. 1–2 раздела «Один проход крышкой».
- Не баг: **`sfltool dumpbtm` виснет** (0% CPU, без вывода, убит по таймауту дважды) — вероятно, терминалу не хватает Full Disk Access. Регистрация login item проверена в Block 07 через System Settings ▸ Login Items и работает; `sfltool` нужен был только как способ проверки, а не продукту. Разбираться, только если понадобится автоматизация.

## Технический долг (код)

Пока пусто — кода нет. Сюда пишутся сознательные срезанные углы по мере появления.

## Идеи после v1.0 (не трогать до релиза)

- [ ] Профили под приложение: в Zoom одно поведение, в Music другое.
- [ ] Непрерывное управление: угол как громкость или яркость. Заманчиво, но у жеста нет обратной связи, кроме экрана, на который в этот момент не смотрят, — нужен отдельный спайк.
- [ ] Промежуточные зоны сверх двух активных.
- [ ] Русская локализация и далее по спросу.
- [ ] Собственная Shortcuts-акция «Set lid zone action», чтобы настраивать Ajar из шортката.

## GTM (после релиза)

Постоянного канала у этого продукта нет — поиска в App Store, который кормит watchcat, здесь не существует. Значит всё держится на первом всплеске и на том, чтобы этот всплеск оставил после себя страницу, которую находят.

- [ ] **Гифка — единственный настоящий маркетинг.** Она объясняет продукт за две секунды и разлетается сама. Всё остальное — обвязка вокруг неё.
- [ ] Show HN. Аудитория точно совпадает: любители нестандартного железа. Формулировка — про неожиданный сенсор, а не про «утилиту для созвонов».
- [ ] r/macapps (там нужна работающая бесплатная версия — она у нас есть), r/apple, MacRumors.
- [ ] Письма мак-блогам про мелкие утилиты: MacStories, Michael Tsai, One Thing Well, подборки Macworld.
- [ ] Страница совместимости с таблицей моделей — она же долгоживущий SEO-актив: люди будут гуглить «does MacBook Air have lid angle sensor» и попадать на нас.
- [ ] Ожидания зафиксировать заранее: один всплеск, дальше плато. Это визитная карточка с окупаемостью сертификата, а не источник дохода. Разочарование планируется так же, как релиз.
